<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/content_holder_distance">
    <TextView
            style="@style/TextViewHeadline5"
            android:layout_width="match_parent"
            android:gravity="bottom"
            android:id="@+id/rv_content_content_title"
            android:text="위치"
            android:fontFamily="@font/nanumbarungothic_bold"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="@dimen/content_title_bottom_margin"
    />
    <ImageView
            android:id="@+id/iv_static_map"
            android:layout_width="match_parent"
            android:layout_height="247dp"
            app:layout_constraintTop_toBottomOf="@id/rv_content_content_title"
            app:layout_constraintBottom_toBottomOf="parent"/>
    <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/md_grey_300"
            app:layout_constraintTop_toBottomOf="@id/iv_static_map"/>
</androidx.constraintlayout.widget.ConstraintLayout>